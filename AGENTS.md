# AGENTS.md — 像素场景装饰工坊

这是一个独立的 git 仓库（`decorator-game/`），发布在
https://github.com/ezcat207/pixel-scene-decorator，部署在
https://decorator-game-six.vercel.app 。父目录（`F:\BaiduNetdiskDownload\整合包`）里还有原始的
546 张场景参考图、一份 docx 素材规范说明、一份 PDF 生产流程指南，那些不属于这个仓库，是本地专属的
参考资料，详见父目录的 `AGENTS.md`。

## 现状（不是"待办"，是已经做完的事）

- 网页装饰游戏本体（`index.html` / `styles.css` / `src/main.js`）：主题+分类筛选素材面板、
  拖拽新增/移动物件、网格吸附开关、图层自动排序（分类优先级 + 纵坐标）、选中工具栏
  （置顶/翻转/删除）、房间尺寸（手动或跟随主题背景图自适应）、localStorage 保存布局、
  导出/导入 JSON、导出房间为 PNG。
- 素材拆分工具，两个版本，算法一致（背景/透明区域连通域检测 → 按连通域切割 → 裁边留白 →
  补齐画布到网格倍数）：
  - `splitter/`：网页版，交互式，可以逐个调整识别结果的名字/分类/取舍，支持通过
    File System Access API 直接写入 `assets/production_sheets/`。
  - `tools/auto_slice.py`：命令行版，用于批量处理、或者没有浏览器交互场景。
- "拼图还原"关卡玩法：只对**本来就是一张图、后来被切开**的素材有意义（比如"八合里火锅"的
  餐台/厨具两张贴纸表）——`auto_slice.py` 切图时会把每个物件在原图里的位置记进
  `assets/production_sheets/<主题>/layouts.json`，`gen_manifest.py` 汇总进
  `manifest.json` 的 `layouts` 字段，前端据此提供"打乱/一键拼好"。
  **这个功能不适用于"素材图鉴/目录表"**（比如把多个独立小动物贴纸排成网格方便查阅的那种图）——
  图鉴里的格子位置不是"正确答案"，硬套这个玩法会得到一个毫无意义的"标准布局"。判断标准：
  这张图本身除了被切开之外，从来没有以"完整画面"的身份存在过／被展示过，才适合做成关卡。
- 主题背景/灵感图：在 `assets/production_sheets/<主题>/` 目录（跟分类文件夹平级）放
  `background.png`（作为房间的不透明底图，房间尺寸自动跟随这张图的像素尺寸）和/或
  `inspiration.png`（侧栏展示的构图参考图，明确标注"不是标准答案"，不做像素级校验）。
  这套是给"素材图鉴 + 一张风格参考成品图"这类主题用的，森林动物主题是现成例子。
- 已经发布：GitHub 公开仓库 + Vercel 公开部署，**真实裁好的素材图片**（八合里火锅、
  森林动物两个主题）已经过素材所有者本人确认可以公开，包含在仓库和线上demo里。

## 素材命名规范（新增素材必须遵守）

```
assets/production_sheets/<主题>/<分类>/<物件名>__<宽>x<高>.png
```

- `主题`：任意文件夹名。
- `分类`：`floor`(地板) | `wall`(墙体) | `furniture`(家具) | `prop`(道具) | `decor`(装饰)，
  五选一，决定默认图层顺序（地板最底，装饰最上）。
- `物件名__宽x高.png`：名称和尺寸用两个下划线分隔，尺寸建议是 64 的整数倍
  （`GRID_UNIT = 64px`）。
- 图片必须是带透明通道的 PNG（RGBA），背景透明。

新增/更新素材后跑：

```bash
python tools/gen_manifest.py
```

只想生成不含真实素材的公开版清单（用于确认 Vercel/公开仓库该带什么）：

```bash
python tools/gen_manifest.py --out assets/manifest.sample.json --only-themes 示例主题
```

## 从"完整场景大图"到"能拖进游戏的素材"，具体怎么做

1. 判断源图是不是已经满足"背景透明/纯色、物件互不重叠、留白足够"——**只有满足这个条件的图**
   才能直接喂给连通域检测算法（`auto_slice.py` / `splitter/`）。不满足的（比如带完整房间墙体
   地板的场景大图、或者带密集文字标注/线稿风格图标的表）硬切只会得到一堆碎片，这是算法的真实
   局限，不是 bug，遇到这种情况应该放弃自动化，如实告知，不要为了凑数硬切。
2. 满足条件就跑 `auto_slice.py`（需要的话加 `--merge-radius` 处理断线笔画，加
   `--min-area` 过滤噪点碎片），或者用 `splitter/` 网页工具人工逐个确认。
3. 如果这张源图本来就是"一整张被切开"的（不是图鉴/目录），`auto_slice.py` 会自动记录
   `layouts.json`，跑完 `gen_manifest.py` 就能在游戏里用"关卡"功能玩拼图还原。
4. 如果这个主题有配套的成品参考图/背景图，按上面"主题背景/灵感图"那节放进去。

## 部署

- GitHub 仓库是公开的：https://github.com/ezcat207/pixel-scene-decorator
- 连了 Vercel 的 Git 集成，push 到 `master` 会自动触发生产环境重新部署。
- `docs/`（PDF 教程的完整转录，含摘录的生成用 Prompt 原文）**故意排除在仓库和部署之外**
  （`.gitignore` / `.vercelignore` 里都写了 `docs/`）——图片素材本身用户已经明确同意公开，
  但那份文字转录属于付费教程内容，没有得到同样的公开授权，不要因为"反正图片都公开了"就顺手
  把它也传上去。
- 本地跑 `vercel --prod` 部署时要注意：这个命令直接打包本地目录上传，**不看 `.gitignore`**，
  只认 `.vercelignore`。改了 `.gitignore` 记得同步检查 `.vercelignore` 是否也要跟着改，
  不然本地 CLI 部署可能会把不该公开的东西传上去（之前真实发生过一次，教训）。

## 明确不做的事（避免过度设计）

- 不做服务器端/多人协同，布局分享靠导出导入 JSON 文件。
- 不做 45°/等距投影支持，只按现有素材的"0透视"俯视角处理。
- 不给"图鉴/目录表"类素材强行套用"拼图还原"玩法（见上文判断标准）。
- 不用真正的图像分割/AI 抠图模型——本地没有可靠的分割模型，连通域检测是刻意选择的、有明确
  适用边界的简单算法，不要试图用色彩聚类/边缘检测之类的手段硬扩展它的适用范围，边界之外的图
  该人工处理就人工处理。
